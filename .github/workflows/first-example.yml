name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v6

        - name: List and read files
          run: |
               echo "My fist github action job"
               ls -latr 
               cat README.md
        - name: Generate ASCII Artwork
          run: cowsay -f dragon "Run for cover, i am a DRAGON ...uurruhrr" >> dragon.txt

        #- name: list files
        #  run: ls -ltr

        
        #- name: Read File
        #  run: cat README.md